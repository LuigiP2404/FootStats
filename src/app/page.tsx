import Image from "next/image";
import Header from "@/components/Header";

export default async function Home() {
  // const data = await getRounds();
  // console.dir(data.response, { depth: null, maxArrayLength: null });

  return (
    <>
      
    </>
  );
}
